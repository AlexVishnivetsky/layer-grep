# --- Parsing code into meaningful chunks (functions/classes) ---
tree-sitter==0.23.2
tree-sitter-python==0.23.6
# tree-sitter-languages is incompatible with tree-sitter>=0.22 (old grammar-loading API,
# see https://github.com/grantjenks/py-tree-sitter-languages) - using separate grammar
# packages built on the new PyCapsule-based API instead, same as tree-sitter-python above
tree-sitter-javascript==0.23.1
tree-sitter-typescript==0.23.2
tree-sitter-rust==0.23.2

# --- Embedding model for semantic search ---
sentence-transformers==3.0.1
# CPU-only models (bge-small, e5-*, bge-m3) work fine on plain torch==2.3.1+cpu.
# On a machine with an NVIDIA GPU you can install a CUDA build - the code detects CUDA
# on its own (torch.cuda.is_available()) and silently uses it if present; without a GPU,
# the heavier models (Qwen3-Embedding-4B/8B) just print a warning and keep running on
# CPU (slower). CUDA install (example for CUDA 12.4):
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# Also: bge-m3 (and any model without .safetensors in its repo) requires torch>=2.6 -
# transformers blocks torch.load for .bin checkpoints for security reasons
# (CVE-2025-32434) on older torch versions.
torch>=2.6.0

# --- Vector store on top of SQLite ---
sqlite-vec==0.1.9

# --- Support ---
numpy==1.26.4
tqdm==4.66.4                    # progress bar while indexing files

# --- MCP wrapper (mcp_server.py) ---
mcp[cli]==1.28.1
